# Dev profile: enables local JWKS endpoint and points JWT validation at it.
# Run with: --spring.profiles.active=dev
app.security.dev-jwks.enabled=true
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:8080/dev/jwks
